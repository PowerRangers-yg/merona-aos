<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="5dp"
    android:paddingHorizontal="30dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_matching_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="물품"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/btn_matching_cancel"
        app:layout_constraintBottom_toBottomOf="@id/btn_matching_cancel"/>

    <TextView
        android:id="@+id/tv_matching_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="거래중,,"
        android:layout_marginEnd="20dp"
        app:layout_constraintTop_toTopOf="@id/btn_matching_cancel"
        app:layout_constraintBottom_toBottomOf="@id/btn_matching_cancel"
        app:layout_constraintEnd_toStartOf="@id/btn_matching_cancel"/>
    
    <Button
        android:id="@+id/btn_matching_cancel"
        android:layout_width="65dp"
        android:layout_height="30dp"
        android:text="요청 취소"
        android:textSize="11sp"
        style="?android:attr/borderlessButtonStyle"
        android:textColor="@color/white"
        android:background="@drawable/purple_radius"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
    
</androidx.constraintlayout.widget.ConstraintLayout>